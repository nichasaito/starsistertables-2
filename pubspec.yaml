name: starsistertables
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.0
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.2  # เพิ่มบรรทัดนี้แล้ว
  firebase_auth: ^5.3.1
  image_picker: ^1.1.2
  firebase_storage: ^12.3.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/Starsister.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/Starsister.png
